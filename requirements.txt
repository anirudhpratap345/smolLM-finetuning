# Core ML libraries
torch>=2.0.0
transformers>=4.35.0
peft>=0.7.0
datasets>=2.14.0
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Training
trl>=0.8.0

# Unsloth (optional but highly recommended - 2x speedup)
# Install via: pip install -q "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# Uncomment the line below if using Colab or manually installing:
# unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Evaluation & monitoring
evaluate>=0.4.0
scikit-learn>=1.3.0
wandb>=0.15.0  # Optional: for experiment tracking

# Web UI
gradio>=4.0.0
streamlit>=1.28.0  # Optional: alternative to Gradio

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Development (optional)
pytest>=7.4.0
black>=23.0.0
